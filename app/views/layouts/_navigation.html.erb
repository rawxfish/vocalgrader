<%= link_to "VocalGrader", root_path, :class => 'brand' %>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    	<%= link_to('Profile', user_path(current_user))%>
    <%= link_to('Logout', signout_path) %>        
    </li>
  <% else %>
    <li>
    <%= link_to "Login with Facebook", signin_path %>  
    </li>
  <% end %>
</ul>